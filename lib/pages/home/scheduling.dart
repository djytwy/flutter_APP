import 'package:flutter/material.dart';

class Scheduing extends StatelessWidget {
  const Scheduing({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('功能暂未开放，敬请期待！' ,style: TextStyle(color: Colors.white),),
    );
  }
}